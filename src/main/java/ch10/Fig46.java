package ch10;


/**
 * @author pfjia
 * @since 2017/12/5 20:24
 */
public class Fig46 {
	/**
	 * Compute optimal ordering of matrix multiplication.
	 * @param c contains the number of columns for each of the n matrices,c[0] is the number of rows in matrix1.
	 *          The minimum number of multiplications is left in m[1][n].
	 *          Actual ordering is computed via another procedure using lastChange.
	 *          m and lastChange are indexed starting at 1,instead of 0.
	 *          Note:Entries below main diagonals of m and lastChange are meaningless and uninitialized.
	 * @param m  
	 * @param lastChange
	 */
	public static void optMatrix(int[] c, long[][] m, int[][] lastChange) {

	}

    public static void main(String[] args) {
        Number[] n=new Integer[5];
        n[0]=new Long(7);
        System.out.println();
    }
}
